from gui import main_gui

if __name__ == "__main__":
    main_gui()
    # parser = argparse.ArgumentParser(description='Convert .osz to .sspm (Rhythia)')
    # parser.add_argument('input', help='Path to .osz file')
    # parser.add_argument('output_dir', help='Directory to save generated .sspm files')
    #
    # args = parser.parse_args()
    #
    # if args.input.endswith('.osz'):
    #     convert_osz(args.input, args.output_dir)
    # else:
    #     print("Please provide a .osz file.")
